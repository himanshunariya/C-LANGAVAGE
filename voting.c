#include<stdio.h>
int main()
{
     int age;
     printf("enter the age of the parone");
     scanf("%d", &age);
     if(age>=18)
{
      printf("the porsone is eligible for voting.");
}
    else
{
     printf("the porsone is not eligible for voting.");
}
      return 0;
}

