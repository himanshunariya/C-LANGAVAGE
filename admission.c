#include<stdio.h>
int main()
{
        int m,p,c,t,s;
		
		printf("maths");
		scanf("%d",&m);
		printf("phy");
		scanf("%d",&p);
		printf("came");
		scanf("%d",&c);
		
		s=m+p;
		t=s+c;
		
		if ((s>=140)&&(t>=188))
		{
		  printf("the candidate is eligable for admission.");
		}
		  else
		{
            printf("the candidate is eligable for admission.");
	return 0;
		}