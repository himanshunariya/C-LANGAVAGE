#include <stdio.h>

int main() {
    int a,b,c=1;

    printf("Enter a value number\n");
    scanf("%d",&a);

             for (b=1;b<a;b++)
	{
             if (a%b==1)
	{          c=1;
   
    }
    }
             if (b)
	{
    printf("%d is a Prime Number\n",a);
    }
	         else
	{
    printf("%d is not a Prime Number\n",a);
	}
     return 0;
}